{
	"name": "Rewritten Dockerfile",
	"build": {
		// Path is relataive to the devcontainer.json file
		"dockerfile": "Dockerfile"
	},

	"postCreateCommand": "pip3 install -r requirements.txt",

	"customizations": {

		"vscode": {
		  "extensions": [
			"ms-toolsai.jupyter",
			"ms-toolsai.jupyter-renderers",
			"ms-python.python"
		  ]
		}
	}
}
